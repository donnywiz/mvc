{% extends 'base.html.twig' %}

{% block title %}Card Game{% endblock%}


{% block content %}
<div class="main-content">

<h1> {{ title }} </h1>


<ul>
    <li>
        <a href="{{ url("deck") }}">card/deck</a>
    </li>
    <li>
        <a href="{{ url("shuffle") }}">card/deck/shuffle</a>
    </li>
    <li>
        <a href="{{ url("draw") }}">card/deck/draw</a>
    </li>
    <li>
        <a href="{{ url("draw_number") }}">card/deck/draw/:number</a>
    </li>
    <li>
    <a href="{{ url("deal_players") }}">card/deck/deal/:players/:cards</a>
    </li>
    <li>
    <a href="{{ url("deck2") }}">card/deck2/</a>
    </li>
</ul>

{{ dump(data.showDeck) }}

</div>
{% endblock %}
